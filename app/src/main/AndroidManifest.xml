<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.webproject.isara">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IsAra">
        <activity android:name=".View.MyAdverts"></activity>
        <activity android:name=".View.CreateAdvert" />
        <activity android:name=".View.ProfileActivity" />
        <activity android:name=".View.SignActivity" />
        <activity
            android:name=".View.LoginActivity"
            android:exported="true" />
        <activity android:name=".View.JobDetailsActivity" />
        <activity android:name=".View.HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>